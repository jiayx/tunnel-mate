[workspace]
members = [
  "apps/tunnel-mate-gpui",
  "crates/tunnel-core",
]
exclude = ["src-tauri"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.3.0-alpha.1"

[profile.release]
lto = "thin"
strip = true
